import React,{useState} from 'react'

function Login() {
    const[user,setuser]=useState("basem")
    const username={user}

    return (
        <div>
  
        </div>
    )
}

export default Login
